<UserControl x:Class="MarkfownEditor.Views.A4PagePreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Width="{Binding PageSettings.Width}"
            Height="{Binding PageSettings.Height}"
            Background="White">
        <Border Margin="{Binding PageSettings.Margins}"
                BorderBrush="LightGray"
                BorderThickness="1">
            <WebBrowser x:Name="PreviewBrowser" />
        </Border>
    </Border>
</UserControl>